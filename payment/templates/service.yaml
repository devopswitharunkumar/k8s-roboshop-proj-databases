apiVersion: v1 
kind: Service 
metadata:
  name: payment-service 
  namespace: roboshop
spec:
  selector:
    app: payment
    project: roboshop
    tier: app
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080  