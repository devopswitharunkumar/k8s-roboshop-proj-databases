apiVersion: v1 
kind: Service 
metadata:
  name: cart-service 
  namespace: roboshop
spec:
  selector:
    app: cart
    project: roboshop
    tier: app
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080  